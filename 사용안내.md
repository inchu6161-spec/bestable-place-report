# 베스트에이블 플레이스 진단 리포트 생성기

직접 네이버 플레이스를 확인하고 입력한 정보만으로 리포트를 만듭니다. 자동 수집, 네이버 API, 로그인, 데이터베이스, 외부 AI 연결은 없습니다. 문구는 준비된 업종별 문장과 입력 조건을 조합해 브라우저 안에서 생성합니다.

## 1. 오늘 바로 실행하기

1. 윈도우에서 이 프로젝트 폴더를 엽니다.
2. `start.cmd` 파일을 더블클릭합니다. 확장자가 숨겨져 있다면 `start`라는 이름의 **Windows 명령 스크립트** 파일입니다.
3. 검은 창이 열립니다. 필요한 파일이 없으면 처음 한 번 설치합니다. 처음 설치할 때는 인터넷 연결이 필요합니다.
4. 브라우저가 열리면 프로그램을 사용합니다. 자동으로 열리지 않으면 주소창에 **http://localhost:5173** 을 입력합니다.
5. 검은 창은 사용 중에 열어 두세요. 사용을 마치면 창을 닫으면 됩니다.

5173번 주소를 다른 프로그램이 사용 중이면 검은 창에 5174 등 다른 숫자가 표시됩니다. 그때는 창에 표시된 **Local:** 주소를 그대로 여세요.

이 컴퓨터에 준비된 실행 도구를 우선 사용합니다. 다른 컴퓨터로 옮겼을 때 Node.js가 없다는 안내가 나오면 아래 설치 방법을 따라 주세요.

## 2. 직접 명령어로 실행하기

### 처음 한 번: Node.js 설치

1. 브라우저에서 https://nodejs.org 를 엽니다.
2. **LTS**라고 표시된 버전의 Windows 설치 파일을 내려받습니다. 이 프로젝트는 Node.js 22.12 이상이 필요합니다.
3. 설치 파일을 더블클릭하고 기본 설정 그대로 다음(Next)을 눌러 설치합니다.
4. 열려 있던 터미널이나 편집기 창을 닫았다가 다시 엽니다.

### 프로젝트 폴더에서 실행

1. 파일 탐색기에서 이 프로젝트 폴더를 엽니다.
2. 위쪽 주소창을 클릭하고 `cmd`를 입력한 뒤 Enter를 누릅니다.
3. 열린 검은 창에 아래 명령어를 한 줄씩 입력하고 Enter를 누릅니다.

```bat
npm.cmd install
npm.cmd run dev
```

`npm.cmd install`은 필요한 실행 파일을 준비하는 명령어입니다. 처음 한 번 실행하면 됩니다.
`npm.cmd run dev`는 프로그램을 켜는 명령어입니다. 다음부터는 이 명령어만 입력하세요.

브라우저 주소창에 **http://localhost:5173** 을 입력하세요. 종료하려면 검은 창에서 **Ctrl+C**를 누릅니다.

## 3. 리포트 만들기

1. 업체명, 업종, 지역을 입력합니다. 플레이스 URL은 선택이며 링크만 표시합니다.
2. 목표 키워드와 순위를 입력합니다. 모르면 **순위 확인 안 됨**을 체크하세요. 빈 키워드는 리포트에서 제외됩니다. 키워드는 최대 5개입니다.
3. 6개 진단 영역에서 좋음·보통·미흡을 하나씩 선택합니다. 참고 요소를 누르면 판단 기준을 볼 수 있습니다.
4. 강점과 문제점 태그를 필요한 만큼 선택합니다. 메모는 선택입니다. **메모도 고객용 리포트에 표시됩니다.**
5. **리포트 생성하기**를 누릅니다. 빠진 필수 입력이 있으면 무엇을 채워야 하는지 안내합니다.
6. 내용을 확인하고 **리포트 전체 복사**를 누른 뒤 문서 등에 **Ctrl+V**로 붙여넣습니다.
7. **인쇄 / PDF**를 누르고 인쇄 창에서 프린터 대신 **PDF로 저장**을 선택하면 파일로 보관할 수 있습니다. 인쇄 옵션의 머리글/바닥글을 끄면 주소와 페이지 부가 정보가 빠져 깔끔합니다.

입력값을 변경했으면 **리포트 생성하기**를 다시 누르세요. 복사와 인쇄는 마지막으로 생성한 리포트를 기준으로 합니다.

**새로고침하거나 창을 닫으면 입력값과 리포트가 사라집니다.** 필요한 리포트는 먼저 복사하거나 PDF로 저장하세요. `입력 내용 초기화`는 확인 창에서 모두 초기화를 선택하면 입력과 리포트를 지웁니다.

강점이 2개보다 적게 확인되면 강점을 지어내지 않고 ‘추가 확인’ 항목을 표시합니다. 모두 양호하다면 우선 과제는 유지·점검을 제안합니다. 우선 과제는 진단 평가, 문제점 수, 키워드 순위를 함께 반영해 서로 다른 영역 3개를 선택합니다.

## 4. 어떤 파일을 수정하나요?

| 파일 | 역할 |
| --- | --- |
| `src/App.jsx` | 입력 항목, 버튼, 리포트 화면 순서와 안내 문구 |
| `src/App.css` | 색상, 글자 크기, 여백, 모바일 화면, 인쇄 모양 |
| `src/scoreCalculator.js` | 진단 영역, 판단 참고 요소, 배점, 점수 계산, 순위 구간 |
| `src/reportGenerator.js` | 자동 분석 문구, 우선순위 선정, 전체 복사 내용 |
| `src/industryTemplates.js` | 업종별 문구, 마지막 운영 지원 서비스 설명 |
| `src/tags.js` | 강점·문제점 태그, 강점별 설명 |
| `src/main.jsx` | 화면을 처음 여는 연결 파일 |
| `index.html` | 브라우저 탭 제목과 기본 페이지 |
| `package.json` | 실행 명령과 필요한 도구 목록 |
| `pnpm-lock.yaml` | 이번 설치에 사용한 도구 버전 기록 |
| `tests/report.test.js` | 점수·순위·리포트 내용 검사 |
| `start.cmd` | 더블클릭 실행 파일 |

문구만 바꾸려면 주로 **reportGenerator.js**, **industryTemplates.js**, **tags.js**를 수정하세요. 화면에 있는 안내 문구는 **App.jsx**에 있습니다. 색상과 디자인은 **App.css**를 수정하세요. 파일 맨 위의 `--navy`, `--blue`가 대표 색상입니다.

## 5. 오류가 나면

| 보이는 상황 | 쉬운 해결 방법 |
| --- | --- |
| `node` 또는 `npm`을 찾을 수 없음 | Node.js LTS를 설치하고 창을 완전히 닫았다가 다시 엽니다. 또는 `start.cmd`로 실행합니다. |
| 파일 설치 중 인터넷 오류 | 인터넷 연결을 확인하고 `npm.cmd install`을 다시 실행합니다. 회사 보안 프로그램이 차단하면 네트워크 관리자에게 npmjs.org 연결 확인을 요청하세요. |
| 브라우저에 연결할 수 없음 | 실행 창이 열려 있는지 확인합니다. `npm.cmd run dev`를 실행한 뒤 Local 주소를 그대로 여세요. |
| 잘못된 폴더 또는 package.json을 찾을 수 없음 | `package.json`이 보이는 이 프로젝트 폴더에서 다시 주소창에 `cmd`를 입력합니다. |
| 순위 오류 안내 | 1, 18, 43처럼 정수만 입력하거나 ‘순위 확인 안 됨’을 체크하세요. |
| 리포트에 방금 바꾼 내용이 안 보임 | ‘리포트 생성하기’를 다시 누르세요. |
| 자동 복사 실패 | 화면에 나타나는 복사용 글을 전체 선택한 뒤 Ctrl+C를 누릅니다. 모바일은 글을 길게 눌러 복사합니다. |
| 설치 도구 파일 누락 안내 | Node.js LTS 설치 후 `npm.cmd install`을 실행하세요. |

## 6. 나중에 웹 주소로 공유하기

프로그램에 업무 데이터를 처리하는 서버는 없습니다. 실행 창은 브라우저에 화면 파일을 전달하는 용도입니다. 입력 정보는 브라우저 안에서만 처리합니다.

Vercel이나 Netlify에 올릴 때 사용할 설정:

- 프로젝트 종류: Vite
- 설치 명령: `npm install` 또는 `pnpm install`
- 완성 파일 만들기: `npm run build`
- 완성 파일 폴더: `dist`

직접 완성 파일을 만들려면:

```bat
npm.cmd run build
```

생성된 `dist` 폴더를 정적 웹사이트로 배포할 수 있습니다. 실제 공개 배포는 이번 작업에 포함하지 않았습니다. 로그인 기능이 없으므로 공개 주소를 아는 사람은 누구나 도구를 열 수 있습니다.

점수와 문구 검사는 `npm.cmd test`로 실행합니다. 복잡한 설치 없이 브라우저 안에서 사용할 수 있도록 외부 글꼴과 이미지 요청도 사용하지 않습니다.

### 이번 컴퓨터에서 확인한 설치 연결 문제

기본 설치 사이트 연결이 끊겨 이번에는 npm 미러에서 동일한 패키지를 받아 설치를 완료했습니다. 이미 설치되어 있으므로 지금은 `start.cmd`만 실행하면 됩니다. 다른 컴퓨터에서 같은 연결 오류가 반복되면 아래 명령어로 설치를 다시 시도할 수 있습니다.

```bat
npm.cmd install --registry=https://registry.npmmirror.com
```

### 완료한 검사

- 점수·순위 경계값 및 리포트 검사 7개 통과
- 6개 업종 × 729개 평가 조합, 총 4,374개 리포트 생성 확인
- 실제 Chrome에서 필수 입력 안내, 키워드 최대 5개, 미확인 순위, 전체 복사, 재생성 안내, 초기화 확인
- PC 및 휴대폰 크기 화면, 인쇄용 출력 확인
- 배포용 `dist` 폴더 생성 성공
